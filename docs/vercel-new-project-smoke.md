# Vercel new-project smoke trigger

This file exists only to trigger the GitHub -> Vercel integration on the newly created Vercel project. It does not change application behavior, UI, APIs, SOS, PWA, or runtime configuration.
